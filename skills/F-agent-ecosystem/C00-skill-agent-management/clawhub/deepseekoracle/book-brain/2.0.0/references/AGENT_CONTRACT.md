# BOOK BRAIN — Agent contract

Read `SECURITY.md` first.

## You may

- List directories and propose BOOK BRAIN layout
- Run `scaffold_haven.py --dry-run` and show the plan
- Create **missing** folders and **new** index/ref files after user says yes
- Append to `memory/YYYY-MM-DD.md` and write topic snips with URLs/SHAs/IDs only
- Point user to `lyra-brain` for graph grow after snips

## You must not

- Delete, rename, or bulk-overwrite existing user files
- Store secrets in stubs or dailies
- Claim LATTICE ALIGNED without stack verify when logging health refs
- Auto-run scaffold without dry-run review

## Session close (with lyra-brain)

1. `memory/YYYY-MM-DD.md` — index table
2. `memory/YYYY-MM-DD-<slug>.md` — DONE bullets + links
3. `reference/` or `memory/reference/*.ref.txt` — outer pointer
4. Optional: `lyra-brain/scripts/session_log_snip.py --grow`

**Δ9Φ963**