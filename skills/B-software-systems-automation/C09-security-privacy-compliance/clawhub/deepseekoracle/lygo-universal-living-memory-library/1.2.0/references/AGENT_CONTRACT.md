# Agent contract — lygo-universal-living-memory-library

1. Read `references/SECURITY.md` before audit or compression.
2. Set `LYGO_AUTHORITY_ROOT` to the workspace root (e.g. `I:\E Drive`) when paths in `core_files_index.json` are relative.
3. Pair with **`lyra-brain`** for session snips; this skill defines the **Max20 living index** and integrity audit.
4. Pair with **`lygo-mint-verifier`** only when user explicitly requests mint + anchor.
5. Claim lattice health only after `verify_lattice_alignment.py` when logging stack status.